{
  "name": "@skidding/linked-list",
  "version": "2.0.0",
  "description": "Turn an array into a stateless linked list",
  "repository": "https://github.com/skidding/linked-list",
  "author": "Ovidiu Cherecheș <hello@ovidiu.ch>",
  "license": "MIT",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "prettify": "prettier --write \"src/**/*.{js,jsx,ts,tsx,json,css,md}\"",
    "check-types": "tsc --noEmit",
    "test": "jest --coverage",
    "test:watch": "jest --watch",
    "build": "rm -rf dist && yarn tsc -b tsconfig.build.json"
  },
  "devDependencies": {
    "@types/jest": "^27.4.0",
    "jest": "^27.4.7",
    "prettier": "^2.5.1",
    "ts-jest": "^27.1.3",
    "typescript": "^4.5.4"
  }
}
